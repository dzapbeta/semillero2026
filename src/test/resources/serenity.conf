# Configuración de Serenity. Se lee sola al ejecutar las pruebas.
serenity {
  # Nombre que aparece arriba en el reporte HTML.
  project.name = "Clase 3 - Screenplay API y Data Driven"
  # En API no hay navegador, así que no hay capturas de pantalla.
  take.screenshots = DISABLED
}
